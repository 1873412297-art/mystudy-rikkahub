# 2.4.5 移植状态（2026-08-07）

## 目标
把私有 fork 的功能栈移植到官方最新版本 **2.4.5（versionCode 172）**，
使私有功能（群聊、酒馆、状态渲染、Slash、群组上下文玩法）运行在 2.4.5 基座上。

## 已完成

- 分支：`codex/port-private-to-2.4.5`（worktree：`C:\Users\18734\Desktop\HTML\rikkahub-source-2.4.5`）
- 基座：`upstream/2.4.5`（`5f39f1c1`）
- 采用**受控 merge**（AGENTS.md 允许）而非逐提交 rebase：
  `git merge codex/port-private-to-2.4.1` → `ac985de2`（parent: 5f39f1c1, adb6e50d）
- 冲突解决要点：
  - 6 处 strings.xml：上游 key 全保留 + 私有 key 追加
  - 3 个 build.gradle.kts：追加私有测试依赖（并修复丢失的 `}`）
  - 5 个测试文件冲突：取 2.4.5 侧（上游测试更新版）
  - `RouteActivity.kt`：2.4.5 + 私有 Screen（TavernCardViewer/Editor、AssistantGroupMembers、SettingPreferencesRuntime）+ onSaveInstanceState Bundle 防护 + Chat.greeting 参数
  - `AppModule.kt`：2.4.5 + StatusVariableStore/StatusRenderer；丢弃 AILoggingManager / RemoteConfig / 旧 Highlighter
  - `ChatService.kt`：2.4.5 + statusVariableStore 注入；恢复上游 `backgroundTextGenerationParams` 默认 `ReasoningLevel.AUTO`（`b70a39eb`）
  - `TavernCardViewerPage.kt`：WebView 预览改用 2.4.5 的 `WebViewContentCache.store` + `contentId`
- DB schema：保持合并后的 v27 链（24=上游 folder_id → 25=+status/group → 26 → 27=+group_runtime_state），Room 校验通过

## 验证（已在 2.4.5 移植分支上执行）

1. `./gradlew :app:compileDebugKotlin -x :web:buildWebUi`：BUILD SUCCESSFUL
2. `./gradlew :app:testDebugUnitTest`：**44 测试类 / 259 测试，0 失败 / 0 错误**
3. `./gradlew :app:assembleDebug`（含 web-ui）：**BUILD SUCCESSFUL**，产出 3 个 ABI APK

## 待办（下一步）

把 private-main 独有的提交与 WIP 快照移植到 2.4.5 分支：

- private-main 相对 port-2.4.1 的 32 个提交（跳过 docs/merge）：
  - 提示词追踪控制台（`4d21c10e`..`1662eed1`，约 21 个提交）
  - 状态 HUD（`5bf9d53e`、`fc970e7f`）
- WIP 快照 `794ca7d9`（酒馆功能包：世界书/ST 正则/QuickReply/作者注释/脚本 API）
- 注意：private-main 的 Room v28/v29 迁移 replay 到 2.4.5 移植分支时需要保留 folder_id 列
  （与本次 merge 处理 25/26/27.json 相同的方式）

## 备份/安全

- private-main WIP 已快照到分支 `codex/private-main-wip-snapshot-2026-08-07`（提交 `794ca7d9`）
- 主工作区 `private-main` 未被触碰（除快照分支切换）
