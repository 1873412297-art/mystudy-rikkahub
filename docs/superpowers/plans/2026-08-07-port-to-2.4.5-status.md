# 2.4.5 移植状态（2026-08-07 更新：全量完成）

## 目标
把私有 fork 的功能栈移植到官方最新版本 **2.4.5（versionCode 172）**。

## 移植内容（全部完成）

分支：`codex/port-private-to-2.4.5`（worktree：`C:\Users\18734\Desktop\HTML\rikkahub-source-2.4.5`）
基座：`upstream/2.4.5`（`5f39f1c1`）

1. **私有功能栈**（受控 merge `codex/port-private-to-2.4.1` → `ac985de2`）：
   群聊（成员/轮询/主持人/群组上下文玩法）、酒馆角色卡、状态渲染、Slash 引擎、群组运行时
2. **提示词追踪控制台**（21 提交 replay，含 `12c97e1f` 冲突解决：GenerationHandler 重构 + trace 记录）
3. **状态 HUD**（`5bf9d53e`/`fc970e7f` replay：StatusBlock 标签族 + StatusHudBar）
4. **酒馆功能包 WIP**（`794ca7d9` replay：世界书 ST 语义、ST 正则、QuickReply、作者注释 @Depth、脚本 API 扩展）

冲突解决要点（本次追加）：
- `GenerationHandler.kt`：12c97e1f 重构冲突取 theirs；文件被截断后按 private-main 尾部 + 2.4.5 尾部重建；
  `contextMessageSize` → `contextMessageLimit`（2.4.5 字段名）
- `RouteActivity.kt`：多次整文件冲突 → 取 ours（2.4.5 基）+ 追加私有 Screen/入口
  （TavernPromptConsole、Lorebooks 等）
- `strings.xml`：统一重建 = 2.4.5 + private-main 私有 key，根标签补 `xmlns:tools`
- `StatusHudBar`：酒馆运行时参数随 WIP 一并就位

## 验证（已在 2.4.5 移植分支上执行）

1. `./gradlew :app:compileDebugKotlin -x :web:buildWebUi`：**BUILD SUCCESSFUL**
2. `./gradlew :app:testDebugUnitTest`：**70 测试类 / 518 测试，0 失败 / 0 错误**
3. `./gradlew :app:assembleDebug`（含 web-ui）：**BUILD SUCCESSFUL**
   - `app-arm64-v8a-debug.apk` / `app-universal-debug.apk` / `app-x86_64-debug.apk`

## 待办

- 模拟器烟雾测试（当前无 `emulator-5554` 在线；参考 AGENTS.md 的群聊/酒馆/状态 HUD 冒烟清单）
- 合入 `private-main`（按 AGENTS.md 工作流，先提交 private-main 当前内容，再受控合并此分支）

## 备份/安全

- private-main WIP 快照：`codex/private-main-wip-snapshot-2026-08-07`（`794ca7d9`）
- 主工作区 `private-main` 未被改动
